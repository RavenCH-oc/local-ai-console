# Copy this file to .env only when a later development phase needs local settings.
# Keep .env private and never commit credentials, host details, or runtime paths.

# This baseline intentionally defines no active runtime configuration.
LOCAL_AI_CONSOLE_ENV=development
