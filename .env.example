# Copy this file to .env only for private, machine-specific local settings.
# .env is always private and must never be committed.

# Optional Controller Runtime root override. Leave blank to use the platform default.
# This example must not contain a real user path, host detail, credential, or model path.
LOCAL_AI_CONSOLE_HOME=
