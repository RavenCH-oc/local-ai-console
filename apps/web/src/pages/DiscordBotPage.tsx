export function DiscordBotPage() {
  return (
    <section className="page" aria-labelledby="discord-heading">
      <p className="eyebrow">Planned service</p>
      <h1 id="discord-heading">Discord Bot</h1>
      <p className="page-lead">Planned service and runtime status management.</p>
      <p>No credentials, SDK integration, endpoint selection, or bot behavior is configured in this phase.</p>
    </section>
  );
}
